import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#000000',
      contrastText: "#FFFFFF"
    },
    secondary: {
      main: "#1ED761"
    }
  },
});

export default theme;
